import os
import requests

api_key = os.environ.get("LLM_API_KEY", "EMPTY") # Wait, I don't have the API key in the sandbox!
# But I can just check the fireworks public docs or unauthenticated endpoints if any?
